import { style } from '@vanilla-extract/css';

export const nextPRToReviewContainer = style({
  width: '100%',
  marginBottom: '20px',
  padding: '20px',
  background: '#f1f1f1',
  borderRadius: '10px',
});
